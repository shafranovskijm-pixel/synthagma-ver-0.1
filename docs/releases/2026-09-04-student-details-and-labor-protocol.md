# Кабинет организации и протокол охраны труда — 4 сентября 2026

## Статус выпуска

Это запись проверенных этапов, не подтверждение полной готовности ГОРЭЛТЕХ.
Интерфейс ученика и создание/прохождение курса не меняются данным релизом.

### P1: доставлен на публичный сайт

- GitHub `main` при проверке 4 сентября: `c7c1d5d73b546c0fa19e7179f4beef1975ab6c55`.
- Исправления: ошибки загрузки личного дела не превращаются в пустые данные;
  история входов и заходов в курсы имеют независимые ошибки;
  результаты тестов ограничены организацией до применения лимита.
- В изолированной сборке Node 20.20.2 с `npm ci`: app TypeScript прошёл,
  207 файлов / 1562 теста прошли, production build прошёл.
- На публичном домене цепочка HTML → `index-DzzQWuK4.js` → `App-_R4exftE.js`
  → `OrganizationDashboard-CJwjpiiE.js` отвечает HTTP 200 с JavaScript.
  В связанном `FRDOExportDialog-CG0ibtdG.js` найдены новые P1-сообщения ошибок.
- Во время проверки изменился entry-файл. Отсутствие старых чанков не означает,
  что свежая цепочка сборки сломана. Перед диагностикой необходимо читать новый HTML.
- Точный SHA сборки на сервере не доказан: локальные и production asset hashes различаются.
  Наличие нового кода подтверждено, но это не заменяет runtime/E2E под организацией.

### Следующий кандидат: локальный, ещё не production

Коммит `0924c863242dcbba6b9e661ba64cb8cd6b14bb40` включает:

1. Независимый блок XML при ошибке загрузки личных документов.
2. Отдельную форму протокола охраны труда: номер, дата проверки знаний,
   явно выбранный результат «Сдал» / «Не сдал».
3. Сохранение через RPC с проверкой организации, прав и версии записи.
   Успех показывается только после повторного чтения точной записи.
4. Отсутствие автоматической подстановки даты завершения и результата «Сдал».
5. Сохранение исходных IDs и снимков ФИО/курса после удаления зачисления.
6. Реальное повторение загрузки XML-карточки после ошибки, без изменения
   родительских зачислений и без повторных запросов от двойного клика.

Новый протокол не создаёт удостоверение или запись о выдаче удостоверения.
Старый номер из журнала обозначается как неподтверждённый источник;
он не подтверждает дату и результат проверки знаний.

Проверки кандидата `0924c8632`: app TypeScript и production build прошли.
Полный набор дал 1603 passed / 1 failed: проверка нумерации превысила 5 секунд
на чтении всех 567 миграций. Без изменений изолированный повтор прошёл 14/14.
Следующим изменением поиск переведён на newest-first с остановкой на последнем
определении функции; проверки порядка блокировки и чтения версии сохранены.
Полный повтор для исправленного кандидата обязателен.

Добавлен независимый `/build-info.json`: точный Git HEAD, состояние tracked-файлов
до запуска генераторов сборки и время начала сборки. Без Git или при ошибке
поле явно `unknown`; marker не подменяет номером сборки SHA и не меняет
существующий механизм runtime cache/reload. После публикации проверять JSON
с cachebust, а не считать любой HTTP 200 доказательством: SPA может вернуть HTML.

### Полный повтор пройден

Точный проверенный кандидат: `fbdde291781fd531ec5191521c28f858224e541f`.
На Node 20.20.2 и 1273 установленных пакетах, соответствующих package-lock:

- `tsc -p tsconfig.app.json --noEmit`: exit 0.
- Тест нумерации и marker: 27/27.
- Полный Vitest с `--maxWorkers=4`: 213 файлов, 1617/1617 тестов, exit 0.
- Production build: exit 0, 6453 модуля.
- Локальный `dist/build-info.json`: exact Git SHA кандидата, `trackedDirty: false`,
  `gitStateCapturedAt: "before-build"`, `builtAt: "2026-09-03T21:41:24.029Z"`.
- Генерируемое сборкой изменение MCP восстановлено; проверочный worktree чистый.

Эти результаты относятся к кандидату, не доказывают установленную production-миграцию
или успешный авторизованный сценарий на сайте. Последующие изменения этого отчёта
и карты разрыва ГОРЭЛТЕХ не изменяют код проверенного кандидата.

## Независимый выпуск без миграции

Отдельно проверен и отправлен в `main` коммит
`45c8529afcfc3e4dd386f9414c4d0ab5f50b6bdb`: исправление границы ошибок личных
документов и команда настоящей проверки типов. Миграция и новая форма протокола
в него НЕ входят; подготовленный кандидат остаётся в ветке
`codex/goreltech-release-gate-20260904`.

Точные проверки `45c8529af`: app TypeScript exit 0; 209 файлов,
1574/1574 тестов; production build exit 0; проверочный worktree чистый.
После push `git ls-remote` подтвердил полный SHA в `refs/heads/main`.
Push сам по себе не доказывает публикацию — отдельно проверяется публичный
JS с текстом «Раздел XML по охране труда доступен отдельно».

После push выполнены три свежие проверки публичной цепочки с интервалом
45 секунд. Во всех случаях сайт продолжал отдавать прежнюю сборку:
`index-DzzQWuK4 → App-_R4exftE → OrganizationDashboard-CJwjpiiE → FRDOExportDialog-CG0ibtdG`.
Все ресурсы — JavaScript, HTTP 200; уникальный текст нового исправления отсутствует.
Следовательно, публикация `45c8529af` на production пока НЕ подтверждена.
Повторный деплой вслепую не запускался; дальнейшие опросы остановлены.

Последующая отдельная проверка 4 сентября подтвердила доставку P2-исправления:
свежий HTML с cachebust ведёт по цепочке
`index-P0e8z6eY.js → App-Cu1yJv_T.js → OrganizationDashboard-DyweD8ZB.js → FRDOExportDialog-D7vtENtv.js`.
Все четыре ресурса отвечают HTTP 200 с типом JavaScript. В последнем чанке
присутствует уникальный текст «Раздел XML по охране труда доступен отдельно».
Таким образом, доставка поведения P2 подтверждена публичным артефактом;
точный SHA production-сборки независимо через manifest не доказан.
Это последующее наблюдение не отменяет три предыдущие проверки старой сборки
и не заменяет авторизованный runtime/E2E или проверку БД.

Nginx preflight для `/sb-functions/compile-group-class-journal`:
HTTP 204, разрешён Origin `https://xn--80aaiswd0ak.xn--p1ai`, разрешён
`x-sintagma-required-compiler-revision`, открыт `x-sintagma-compiler-revision`.
Эта проверка не подтверждает авторизованное обращение к БД или сохранение пакета.

## База данных: проверка и порядок установки

Миграция: `supabase/migrations/20260904090000_labor_safety_enrollment_protocols.sql`.

На отдельной PostgreSQL 17.11 выполнены реальные SQL-проверки: запрет прямых
записей, изоляция организаций, проверка зачисления, обязательные дата/результат,
конкурентное обновление по версии, неизменяемость источника и сохранение записи
при удалении зачисления/автора. Auth и `can_access_organization` в этой проверке
подменены тестовыми реализациями — production-права этим не подтверждены.

Локальные исходники/вывод проверки:
`D:/CodexTmp/labor-protocol-pg-proof-20260904/{fixture.sql,contract.sql,contract-output.txt}`.
Кластер остановлен. Тестовые SQL нельзя запускать в рабочей базе.

Порядок выпуска:

1. Завершить полный app TypeScript / tests / build для точного кандидата.
2. Через авторизованный доступ подтвердить нужный Supabase-проект
   `atxwvjxbqjgkbjlhsdch`, применить только новую проверенную миграцию.
   Не выполнять вслепую все накопленные миграции.
3. Проверить доступность таблицы/RPC и права владельца, разрешённого сотрудника,
   сотрудника без записи и другой организации.
4. Опубликовать фронтенд кандидата.
5. В тестовом зачислении сохранить протокол без удостоверения, перезагрузить
   страницу и подтвердить номер, дату и результат; проверить конфликт версий.
6. Скачать XML и сверить значения с сохранённым протоколом. Отдельно проверить,
   что документы/курсы/ученик продолжают открываться при ошибках смежных разделов.

До шага 2 форма сообщает о недоступном обновлении базы; её нельзя выдавать
клиенту как уже работающую production-функцию.

## Что остаётся незавершённым

- Официальная XSD Минтруда не получена и совместимость XML/API не подтверждена.
  Текущий `<LaborSafetyRecords>` — внутренний черновик, пометка Beta обязательна.
- Отбор курсов пока использует сохранённую категорию «Охрана труда».
  Курс с другим названием категории не должен молча считаться проверенным для выгрузки.
- Авторизованный end-to-end ГОРЭЛТЕХ: сохранить пакет из 9 документов,
  скачать и визуально проверить каждый файл против исходных шаблонов.
  Успешный dry-run от 3 сентября не заменяет эту проверку сохранения.
- Авторизованный повторный тест зачисления, результатов и экспорта через Nginx.
- Полная приёмка кабинета организации и клиентские инструкции.

## Откат

Фронтенд можно вернуть к последнему подтверждённому коммиту отдельным релизом.
Новую таблицу при откате фронтенда не удалять: она может уже хранить протоколы.
Не откатывать и не перезапускать Nginx/базу ради обновления статической сборки.
