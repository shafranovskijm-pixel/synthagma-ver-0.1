## Что исправлю

1. **Подтвержу источник проблемы в данных и UI**
   - Перепроверю загрузку списка учеников для организации и для карточек курсов.
   - Сверю, где ученик может пропасть: из-за архива, фильтров, удаления записи или ошибки запроса.

2. **Уберу ложное “исчезновение” завершивших обучение**
   - Исправлю текущую логику, где ученик автоматически считается архивным, если у него все зачисления завершены.
   - Сделаю архив только явным действием, а завершивших оставлю видимыми в основном списке с корректным статусом.
   - Проверю, чтобы фильтры по статусу и курсу не прятали таких учеников неожиданно.

3. **Защищу учеников от необратимой потери**
   - Заменю опасные прямые удаления профиля/зачислений на более безопасный поток.
   - Где нужно, переведу удаление в мягкое скрытие/архив вместо физического удаления.
   - Уберу дублирующиеся места в коде, где профиль и зачисления удаляются напрямую из клиента.

4. **Сделаю загрузку списков единообразной**
   - Приведу основной список учеников и списки по курсам к одной логике получения данных.
   - Исключу расхождения, когда в одном месте ученик виден, а в другом нет.
   - Проверю проблемные места с `.single()`, чтобы отсутствие строки не ломало отображение.

5. **Добавлю проверяемый след изменений**
   - Укреплю журналирование действий по ученикам и зачислениям, чтобы было видно, когда запись архивировали, восстановили или убрали из курса.
   - Это поможет быстро находить причину по конкретному ученику в следующий раз.

## Уже подтверждено по текущему расследованию

- В организации **«Владивостокский центр охраны труда»** найден курс **«Специалист в области охраны труда»**.
- По ФИО **Газукина Аксана Николаевна** и логину **`gazukina_a_n`** в текущих `profiles`, `enrollments`, `lesson_progress`, `test_attempts`, `labor_safety_profiles`, `audit_logs`, `course_access_log`, `enrollment_history` следов не найдено.
- В коде обнаружена критичная причина путаницы:
  - `src/hooks/useStudents.ts` автоматически отправляет ученика в архив, если все его курсы завершены.
  - `src/components/organization/tabs/StudentsTab.tsx` показывает это как отдельную вкладку «Архив», из-за чего для организации ученик выглядит “исчезнувшим”.
- Также найдены рискованные места, где ученик удаляется **физически** из `profiles` и `enrollments` прямо из клиентского кода.

## Технические изменения

- `src/hooks/useStudents.ts`
  - убрать автоархивацию по факту завершения всех курсов;
  - оставить архив только по `profiles.archived_at`;
  - пересчитать активный/архивный списки и фильтры.

- `src/components/organization/tabs/StudentsTab.tsx`
  - скорректировать подписи и поведение вкладок/фильтров, чтобы завершившие не уезжали незаметно в архив.

- `src/api/students.ts`
  - централизовать безопасные действия над учеником;
  - убрать/ограничить физическое удаление как дефолтное поведение.

- Дублирующие клиентские удаления
  - `src/hooks/useStudentManagement.ts`
  - `src/hooks/useStudentActions.ts`
  - другие найденные места с `.delete()` по `profiles` / `enrollments`
  - заменить на единый безопасный путь.

- База данных
  - при необходимости добавлю безопасный механизм истории/мягкого удаления через миграцию, чтобы изменения были восстановимы и отслеживаемы.

## Результат

После внедрения:
- завершившие ученики не будут выглядеть как пропавшие;
- организация будет видеть их в основном списке;
- удаление станет контролируемым и отслеживаемым;
- по конкретному ученику можно будет быстрее понять, он завершил курс, архивирован или реально удалён.