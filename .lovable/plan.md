

# Переработка раздела «Документы» + «Плательщики» + Админ-биллинг

## Что будет сделано

### 1. Переименовать «Договоры» → «Документы Синтагма»
В `DocumentsTab.tsx` первый пункт sidebar переименовать с "Договоры" на "Документы Синтагма". Описание: "Договоры, счета и закрывающие документы с платформой".

### 2. Добавить раздел «Плательщики» в sidebar
Новый пункт навигации после «Документы Синтагма» — «Плательщики». Это будет интерфейс взаиморасчётов с учениками/компаниями (как на референсе):
- Таблица плательщиков: наименование, ИНН, телефон, почта
- Фильтры: Все / Физ. лица / Юр. лица / Активные / В архиве
- Кнопка «Добавить плательщика»
- Возможность отправки документов (договор, счёт, акт, согласие) ученику в ЛК

**Миграция**: создать таблицу `org_payers` (id, organization_id, name, inn, phone, email, payer_type enum (individual/legal_entity), status, created_at) + RLS.

### 3. Переработать AdminBillingOverview
Расширить компонент `AdminBillingOverview.tsx`:
- Добавить вкладку **«Договоры»** — список из `org_contracts`, кнопка «Создать договор» → выбираешь организацию и отправляешь ей договор (он появляется у орга в «Документы Синтагма → Договоры»)
- **Счета** — уже есть, показывают все `subscription_invoices` по всем оргам
- **Закрывающие** — уже есть, `org_billing_documents` по всем оргам

### 4. Админ-панель: профиль в правом верхнем углу
В `AdminDashboard.tsx` добавить desktop-header с:
- Заголовком текущего раздела слева
- Аватаром/email админа и dropdown-меню (профиль, выход) справа
- Перенести «Обновления» (updates) в этот dropdown или в header как bell-icon

### 5. Подвал админки
Переработать footer в `AdminSidebar.tsx`: вместо просто email + выход — компактный блок с аватаром, email и кнопкой настроек. Кнопку выхода оставить.

## Файлы

| Файл | Изменение |
|---|---|
| SQL миграция | Создать `org_payers` + RLS |
| `src/components/organization/tabs/DocumentsTab.tsx` | Переименовать billing → "Документы Синтагма", добавить nav item "Плательщики" + UI таблицы плательщиков |
| `src/components/admin/AdminBillingOverview.tsx` | Добавить вкладку «Договоры» с кнопкой создания/отправки договора организации |
| `src/pages/AdminDashboard.tsx` | Добавить desktop-header с профилем админа в правом верхнем углу |
| `src/components/admin/AdminSidebar.tsx` | Переработать footer — аватар + email + настройки |

