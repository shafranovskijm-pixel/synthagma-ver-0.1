

## План: изменить порядок — тесты решаются первыми

### Текущий порядок в `processCourse` (строки 212–347):
1. Загрузка уроков
2. Генерация структуры (без тестов)
3. Заполнение контентом
4. Решение тестов
5. Исправление дубликатов
6. Валидация

### Новый порядок:
1. Загрузка уроков
2. **Решение существующих тестов** (блок строк 278–326 переносится сюда, сразу после загрузки)
3. Генерация структуры (`.filter(gl => gl.type !== "test")` — тесты не создаются)
4. Заполнение контентом
5. Исправление дубликатов
6. Валидация

### Проверка безопасности тестов:
- **Строка 235**: `gl.type !== "test"` — AI-сгенерированные тесты отфильтровываются, не вставляются
- **Нигде** нет `.delete()` для уроков — тесты не удаляются
- **Строки 281–314**: тесты только `select` + `update correct_answer` — структура не меняется
- Тесты решаются до генерации структуры → ответы зафиксированы до любых изменений

### Изменение — один файл:

**`src/components/admin/BulkPipelineWidget.tsx`**:
- Вырезать блок «Solve unanswered test questions» (строки 278–326)
- Вставить его сразу после строки 217 (после загрузки уроков, до генерации структуры)
- Использовать `currentLessons` вместо `allLessons` для поиска тестов на этом этапе

