

# Изменения в подвале

## Что будет сделано

1. **Убрать акцентную линию** — удалить декоративную полоску-разделитель в самом верху футера (строка 10-11, `via-[hsl(174,72%,46%)]/30`).

2. **Вернуть взаимодействие с темой** — заменить хардкод-цвета обратно на CSS-переменные темы:
   - `bg-[hsl(222,47%,6%)]` → `bg-background`
   - `text-white` → `text-foreground`
   - `text-white/60` → `text-muted-foreground`
   - `text-white/40` → `text-muted-foreground/60`
   - `bg-white/10` → `bg-foreground/10`
   - и т.д.
   
   Подвал будет адаптироваться к выбранной теме (светлой/тёмной). Звёзды на `StarfieldCanvas` останутся — в тёмной теме они будут ярко видны, в светлой — менее заметны, но фон будет соответствовать теме.

## Файл
- `src/components/landing/Footer.tsx` — убрать линию + заменить все `text-white`/`bg-[hsl(...)]` на тематические классы.

