

# Попап «Специальные условия» — только для незарегистрированных

## Что меняем

1. **Таймер с 30 сек → 5 минут** — `DELAY_MS = 300000`
2. **Проверка авторизации** — добавить `supabase.auth.getSession()`. Если пользователь залогинен — попап не показывать
3. **Текст «Только для новых клиентов»** — добавить подзаголовок/бейдж в попап

## Изменения в файле

### `src/components/landing/SpecialOfferPopup.tsx`

- Строка 12: `DELAY_MS = 300000` (5 минут)
- В `useEffect`: проверить сессию через `supabase.auth.getSession()`, если есть user — не показывать
- В форме: добавить текст «Только для новых клиентов» рядом с заголовком или бейджем

