## Изменения

### 1. Перенести «Инструменты для обучения» сразу после тарифов

**`src/pages/Index.tsx`** — порядок секций:
```
<PricingPlans />
<Features />            ← было после BoxedVersionCard/WebsiteDevelopmentCard
<BoxedVersionCard />
<WebsiteDevelopmentCard />
```

### 2. КП в админке — обновить шаблоны под новые услуги

**`src/constants/proposalTemplates.ts`**:

- **Удалить шаблон `maximum`** (тариф убран с витрины, его лимиты переехали в «Профессиональный»).
- **Добавить шаблон `boxed`** — «Коробочная версия» (540 000 ₽, единоразово, 1 шт), `tariffPlan: ''`, features: доработка / установка на сервер / бессрочная лицензия / 3 мес поддержки.
- **Добавить шаблон `website`** — «Сайт под ключ» (55 000 ₽, единоразово, 1 шт), `tariffPlan: ''`, features: Минобрнауки / адаптив / формы / 2–3 недели.
- Шаблон `custom` оставить.

Порядок: `start → standard → professional → boxed → website → custom`.

### 3. Услуги в Sales (каталог `sales_services`)

`src/components/admin/sales/SalesServices.tsx` — там услуги создаются вручную через UI (`createService`). Чтобы у менеджеров сразу были «Коробочная версия» и «Сайт под ключ» в КП-конструкторе, добавлю автосев двух дефолтных услуг при первой загрузке `SalesServices`:

- Если в `services` нет записи с именем «Коробочная версия СИНТАГМА» → создать (price 540 000).
- Если нет «Разработка сайта образовательной организации под ключ» → создать (price 55 000).

Сев один раз через `useEffect` после `fetchServices`, флаг — наличие записи (а не localStorage), чтобы не дублировать.

### 4. Что НЕ трогаем

- БД миграций нет.
- `SUBSCRIPTION_PLANS.maximum` остаётся в коде (для старых организаций).
- Конструктор КП (`ProposalEditor`/`CommercialProposals`) не меняем — он подхватит новые шаблоны и услуги автоматически.

Подтвердите — приступаю.