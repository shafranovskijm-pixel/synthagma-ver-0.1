

## Изменения в кабинете ученика

### 1. Sidebar — упростить и добавить цветовую подложку
- Оставить только 3 кнопки: Каталог, Библиотека, Чат
- Убрать: Магазин курсов, Идентификацию, Согласие, Документы, Достижения, Настройки, Выход
- Расширить sidebar до ~80px
- Добавить цветовую подложку на основе `primaryColor` из branding организации (полупрозрачный фон)
- Активная кнопка — с подписью текстом под иконкой (как в SkillSpace)

### 2. Header — добавить уведомления и перенести всё в профиль
- Добавить иконку колокольчика (уведомления) рядом с аватаром — как в SkillSpace
- В dropdown профиля (клик на аватар) добавить пункты:
  - «Мой профиль» — пока заглушка
  - «Идентификация» → открывает VideoIdentification
  - «Согласие на ПД» → открывает StudentConsentForm
  - «Документы» → открывает StudentDocumentsUpload (с бейджем количества незаполненных)
  - «Достижения» → открывает AchievementsPanel
  - Тема (светлая/тёмная/системная)
  - «Выйти»
- На аватаре показывать бейдж с количеством незаполненных документов (как «3» на скриншоте)

### 3. Footer в кабинете ученика
- Создать компонент `StudentFooter.tsx` — лёгкий подвал как у SkillSpace:
  - Лого/название организации + описание
  - Колонки ссылок: «Платформа» (ссылки), «Документы» (оферта, ПД)
  - Ссылка «Стань партнёром — зарабатывай 25%»
- Разместить внизу контентной области (после курсов)

### 4. Убрать вкладку «Магазин курсов»
- Удалить `"store"` из типа `StudentTab`
- Убрать рендеринг `StudentCourseStore` в `StudentDashboard.tsx`

### Файлы

| Файл | Действие |
|------|----------|
| `src/components/student/StudentSidebar.tsx` | Упростить до 3 кнопок, расширить, добавить цветовую подложку |
| `src/components/student/StudentHeader.tsx` | Добавить колокольчик, перенести идентификацию/документы/достижения в dropdown с бейджем |
| `src/components/student/StudentFooter.tsx` | Создать — подвал в стиле SkillSpace |
| `src/pages/StudentDashboard.tsx` | Убрать store, прокинуть новые пропсы в Header, добавить Footer |

