

## Задача

В разделе «Курсы» (организация) при первом открытии всегда должен показываться **вид сеткой** (grid), а уже после того как пользователь сам переключит на «список» — запомнить выбор и подгружать его в следующий раз.

## Текущее состояние

Нужно посмотреть, как сейчас хранится `viewMode` в `CoursesTab` (вероятно `useState("grid")` без персистентности, либо наоборот — сохраняется и иногда «залипает» на list).
