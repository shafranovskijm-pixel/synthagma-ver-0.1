План исправления:

1. Убрать главный источник «ложной бесконечной загрузки» после регистрации
- Скорректировать production-кэширование и recovery-логику, чтобы после публикации браузер надёжно подхватывал новый фронтенд, а не зависал на старых чанках.
- Проверить и ужесточить сценарий, когда route/chunk не догружается после перехода на `/organization`: вместо бесконечного Suspense-показа давать контролируемое восстановление.
- Сделать поведение одинаково надёжным именно на кастомном домене `sintagma.com.ru`, где у пользователя сейчас воспроизводится проблема.

2. Усилить post-signup переход в кабинет организации
- Доработать переход после `register-organization` + `signInWithPassword`, чтобы при успешной регистрации пользователь не оставался в «подвешенном» состоянии между авторизацией и загрузкой кабинета.
- Добавить более явный fallback на этапе первого входа: если кабинет не успел инициализироваться за разумное время, показывать понятное сообщение и запускать безопасное восстановление, а не оставлять бесконечный спиннер.
- Проверить связку `useRegisterOrganization` → `AuthProvider` → `ProtectedRoute` → `/organization` и убрать место, где возможно зависание без ошибки на экране.

3. Сделать диагностику таких случаев прозрачной
- Добавить логирование/guard для ошибок загрузки lazy-chunk’ов и первого открытия кабинета после регистрации.
- Если загрузка сломана из-за старого service worker / старого asset id / битого chunk cache, выполнять одноразовую автоочистку и повторную загрузку без вечного цикла.
- Оставить пользователю понятный recovery-путь, если автоматическое восстановление не помогло.

4. Проверить сценарий на живом домене
- Повторно прогнать регистрацию новой организации на `sintagma.com.ru`.
- Подтвердить, что после submit пользователь действительно попадает в кабинет организации, а не остаётся на вечной загрузке.
- Отдельно проверить первый экран после входа: onboarding/modal не должны маскировать реальную проблему загрузки.

Что я уже подтвердил
- Бэкенд регистрации сейчас отрабатывает успешно: новая организация создаётся, роль назначается, первый вход проходит.
- На живом домене я воспроизвёл успешную регистрацию и попал в кабинет организации.
- Значит, корень проблемы с высокой вероятностью не в самой регистрации, а в том, что у части браузеров на кастомном домене остаётся старый закэшированный фронтенд или ломается lazy-load после перехода в `/organization`.

Технические правки, которые планирую внести
- `index.html` — усилить recovery для production-кэша / service worker.
- `src/main.tsx` — доработать bootstrap/version-guard, чтобы он надёжнее сбрасывал устаревший фронтенд.
- `src/utils/lazyWithRetry.ts` — убрать сценарий, при котором пользователь остаётся на бесконечном fallback без понятного выхода.
- `src/components/ProtectedRoute.tsx` и/или точку перехода после signup — сделать явный recovery вместо немого зависания.
- При необходимости — добавить диагностические сообщения в `useRegisterOrganization` / auth bootstrap.

После одобрения сразу внесу правки и проверю на `sintagma.com.ru`.