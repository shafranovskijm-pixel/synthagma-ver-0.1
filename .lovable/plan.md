План:

1. **Обновить текст договора в генераторе админки**
   - В шаблонах `Договор возмездного оказания услуг` и `Договор безвозмездного оказания услуг` заменить короткий пункт 1.1 на развернутый предмет договора.
   - Описать, что Исполнитель предоставляет Заказчику и слушателям Заказчика доступ к образовательной платформе СИНТАГМА в режиме 24/7, кроме регламентных/аварийных работ.
   - Добавить функционал тарифа «Старт»: до 15 курсов, до 100 учеников, до 60 обученных в месяц, 3 ГБ хранилища, настройки курсов, чек-лист документов, видеоидентификация, брендирование, ФИС ФРДО, отчеты/журналы, цифровая библиотека, кабинет слушателя, охрана труда, Kinescope-видео, ИИ-генерация/озвучка, email-рассылки.
   - Для безвозмездного договора сохранить формулировку «на безвозмездной основе» и цель без извлечения прибыли.

2. **Сделать предмет договора управляемым, но с правильным дефолтом**
   - В поле «Предмет договора» в форме генератора подставлять новый развернутый текст по умолчанию.
   - Если пользователь вручную меняет предмет, генератор должен использовать его текст, а не затирать дефолтом.

3. **Решить деплой через GitHub / Timeweb**
   - Добавить фиксацию версии Node для продакшн-сборки: `.nvmrc` и/или `.node-version` со стабильной LTS-версией `20`, чтобы Timeweb не пытался собирать проект на Node 24.
   - Добавить `engines` в `package.json`: Node 20.x и npm 10.x как ожидаемое окружение для Vite 5.
   - Добавить/обновить npm-настройку так, чтобы `npm ci` ставил optional dependencies, потому что сейчас в lockfile видны только linux-бинарники `@rollup`/`@esbuild`, а прошлые ошибки Timeweb были именно вокруг optional/platform-зависимостей.
   - Проверить, что `package-lock.json` синхронизирован с `package.json` после правок.

4. **Проверка результата**
   - Проверить, что предпросмотр безвозмездного договора показывает расширенный раздел «1. Предмет договора».
   - Проверить, что проект собирается локально/через стандартную проверку Lovable.

Технически затрону только: `src/lib/adminDocTemplates/index.ts`, `src/components/admin/AdminDocumentsManager.tsx`, `package.json`, `.npmrc`, `.nvmrc`/`.node-version` и при необходимости `package-lock.json`.