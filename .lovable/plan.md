

# Выделение шапки на главной странице + радио

## Что будет сделано

1. **Выделить навигационную полоску** — добавить тонкую верхнюю полосу (top bar) с градиентным или акцентным фоном, отделив её визуально от остального Hero. Это создаст эффект «уголочка» — переливающаяся полоска сверху страницы.

2. **Добавить кнопку Радио** в шапку лендинга — импортировать `RadioPlayerButton` рядом с `ThemeToggle` в навигации Hero, чтобы посетители могли слушать радио прямо с главной.

3. **Визуальное выделение шапки** — навигация получит полупрозрачный фон с лёгким `backdrop-blur` и тонкую акцентную линию сверху (gradient border в teal/cyan), чтобы шапка «отрывалась» от фона и выглядела премиально.

## Технические детали

### Файл: `src/components/landing/Hero.tsx`

- Обернуть `<nav>` в контейнер с фиксированным фоном: `bg-card/80 backdrop-blur-md border-b border-border/30`
- Добавить сверху декоративную полоску 2-3px с градиентом `from-accent via-primary to-accent`
- Импортировать и вставить `<RadioPlayerButton />` рядом с `<ThemeToggle />` в блоке кнопок
- Обернуть в `TooltipProvider` если нужно для корректной работы тултипов радио

