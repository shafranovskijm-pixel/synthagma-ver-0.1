

# Доработка тем: цвета минимализма, группировка, «По умолчанию», вторая тема воды

## Что будет сделано

### 1. `admin-themes.ts` — изменения

- **«Офис с зеленью»**: перенести из `group: "nature"` → `group: "office"`
- **«Минимализм»**: обновить баннер на более точное фото горного озера с фиолетовыми тонами (фиолетовые горы, хвойный лес, отражение в воде — как на скриншоте). Подобрать Unsplash-изображение с глубокими пурпурными/лавандовыми оттенками
- **Новая тема «Океан»** (id: `ocean`, group: `water`): глубокая синева океана с волнами, акцент ~210 80% 55%, анимация `particles`

### 2. `ThemeSelector.tsx` — добавить «По умолчанию»

Перед группами добавить карточку «По умолчанию» — сбрасывает визуальную тему (activeId = null). Визуально: светлая карточка без изображения, текст «По умолчанию». При выбранной теме — не выделена. Без темы — выделена как активная.

### 3. Файлы

- `src/constants/admin-themes.ts` — перенос группы, обновление баннера, новая тема
- `src/components/ui/ThemeSelector.tsx` — карточка «По умолчанию» перед группами

~40 строк изменений, 2 файла.

