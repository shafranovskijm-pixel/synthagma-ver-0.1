

## Анализ

Уже сейчас в конце `processCourse` (строка 347) курс помечается `is_validated: true`. А виджет получает только курсы с `!c.is_validated` (строка 430 в `AdminMarketplaceManager`). После `onComplete` вызывается `h.fetchData()`, который перезагружает данные.

**Проблема**: `onComplete` вызывается только после завершения всего массового прогона (строка 388), но не после тестового прогона. Плюс курс не исчезает из виджета в реальном времени — только после полной перезагрузки.

## План

### Один файл: `BulkPipelineWidget.tsx`

1. **После тестового прогона вызывать `onComplete`** — чтобы `AdminMarketplaceManager` перезагрузил данные и курс переместился из папки «В работе» в «Курсы Ростехнадзора».

2. **После каждого успешного курса в массовом прогоне — тоже вызывать `onComplete`** (или в конце, как сейчас — это уже работает).

Изменение минимальное: добавить `onComplete?.()` в конец `handleTestRun` после успешной обработки.

