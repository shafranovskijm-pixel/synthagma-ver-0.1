

## Перенос «Закрывающие документы» в Документооборот + улучшение визуала

### Что будет сделано

1. **Перенос блока «Закрывающие документы»** из `SubscriptionTab.tsx` в `DocumentsTab.tsx` как новый подтаб «Закрывающие».
2. **Удаление блока из SubscriptionTab** — останется только аккордеон с тарифом (без billing docs card сверху).
3. **Добавление подсказок-плашек** в каждый подтаб DocumentsTab — краткие описания, что делать и зачем, по аналогии с «продажными» плашками в других разделах.

### Изменения по файлам

#### `src/components/organization/tabs/DocumentsTab.tsx`

- Добавить новый подтаб `"billing"` (Закрывающие) в массив `tabs` и тип `DocumentSubTab`.
- Перенести всю логику billing docs:
  - state: `billingDocs`, `showActDialog`, `actDate`, `actBasis`, `actAmount`, `actSubmitting`, `orgDetails`
  - fetch `org_billing_documents` + org details (inn, director_name, director_position) в useEffect
  - `handleDownloadDoc`, `handleGenerateAct`
  - JSX: Card с документами + кнопка «Сформировать акт» + Dialog генерации акта
- Добавить **информационные плашки** (hint cards) в начало каждого подтаба — компактные карточки с иконкой, заголовком и описанием:
  - Конструктор: «Настройте шаблоны документов — реквизиты, печать и подпись будут автоматически подставляться во все генерируемые документы»
  - Документы орг.: «Загрузите обязательные документы организации по 273-ФЗ. Система подскажет, каких документов не хватает»
  - Приказы: «Здесь хранятся сгенерированные приказы о зачислении и отчислении учеников»
  - Протоколы: «Протоколы аттестационной комиссии, сформированные по результатам обучения»
  - Удостоверения / Дипломы / Свидетельства: «Журнал выданных документов об образовании с номерами и датами»
  - Программы: «Управление образовательными программами организации»
  - Закрывающие: «Счета, чеки и акты от платформы. Сформируйте акт выполненных работ для бухгалтерии»

#### `src/components/organization/SubscriptionTab.tsx`

- Удалить billing docs Card (строки 268-313), состояния `billingDocs`, `showActDialog`, `actDate`, `actBasis`, `actAmount`, `actSubmitting`, `orgDetails` и связанные функции.
- Удалить Act Generation Dialog (строки 613-670).
- Убрать `orgDetails` из fetchOrgDetails.
- Аккордеон «Тарифный план» станет единственным содержимым (можно раскрыть по умолчанию, раз больше нечего показывать сверху).

### Визуал подсказок

Компактная плашка перед контентом каждого подтаба:

```text
┌─────────────────────────────────────────────┐
│ 💡 Заголовок                                │
│ Краткое описание что делать и зачем          │
└─────────────────────────────────────────────┘
```

Стиль: `bg-muted/50 border rounded-xl p-4` с иконкой `Lightbulb` или тематической, мягкие цвета.

