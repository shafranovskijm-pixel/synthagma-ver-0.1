

# Заглушки для Материалов и Тестов + создание групп прямо в курсе

## Что будет сделано

### 1. Заглушка «Материалы» — продающее описание
В `CourseDocumentsManager.tsx` заменить пустое состояние (`Нет документов`) на информативную карточку:
- Иконка `FileText` крупная с gradient-фоном
- Заголовок: **«Материалы курса»**
- Описание: «Загружайте учебные материалы, методички, презентации и дополнительные файлы для учеников. Все документы будут доступны ученикам прямо в уроках курса.»
- Кнопка «+ Добавить документ» (уже есть сверху, но продублировать в заглушке для удобства)

### 2. Заглушка «Тесты» — подсказка
В `CourseTestReport.tsx` заменить пустое состояние (`Нет результатов тестирования`) на:
- Иконка `BarChart3` с gradient-фоном
- Заголовок: **«Результаты тестирования»**
- Описание: «Здесь появятся результаты после того, как ученики пройдут тесты. Добавьте тестовые уроки в конструкторе курса, чтобы отслеживать успеваемость.»
- Кнопка «Перейти в конструктор» → navigate на `/course-builder/:courseId`

### 3. Создание групп прямо из вкладки «Группы»
В `CourseGroupsTab.tsx`:
- Добавить кнопку «+ Создать группу» в заглушку и в заголовок списка
- При клике — inline-форма (название группы + цвет) или простой Dialog
- Вызов `supabase.from("student_groups").insert(...)` с `organization_id`
- После создания — обновить список групп
- Обновить заглушку с описанием: «Группы позволяют массово зачислять учеников и управлять расписанием обучения.»

## Файлы

| Файл | Изменение |
|---|---|
| `src/components/organization/CourseDocumentsManager.tsx` | Информативная заглушка вместо «Нет документов» |
| `src/components/organization/CourseTestReport.tsx` | Информативная заглушка вместо «Нет результатов тестирования» |
| `src/components/organization/CourseGroupsTab.tsx` | Добавить создание групп + обновить заглушку |

Миграций не требуется.

