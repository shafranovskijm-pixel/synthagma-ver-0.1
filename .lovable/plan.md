## Цель
Перестроить главную (`/`) под путь собственника учебного центра: запустить СДО → подключить учеников → готовые курсы/ИИ → документы/контроль. Сохранить все существующие сильные блоки, усилить тексты и порядок.

## Итоговая структура страницы (`src/pages/Index.tsx`)

1. `<LandingHeader />` — без изменений
2. `<Hero />` — обновлённый продающий hero (только текст/CTA)
3. **NEW** `<HowItWorks />` — широкая карточка-цепочка + 4 мини-карточки
4. `<EditorDemoSection />` — «Создавайте курсы за минуты» (вытащим из `EditorAndFrdoSections`)
5. `<RostechnadzorCourses />` — «300+ готовых курсов» (текст усилить)
6. `<Features />` — «Все инструменты учебного центра в одной платформе» (текст + раскрытие карточек по клику)
7. `<FrdoSection />` — болевой блок ФРДО (вытащим из `EditorAndFrdoSections`, текст смягчить)
8. `<MobileApp />` — без изменений
9. `<PricingPlans />` — добавить подпись сверху
10. `<BoxedVersionCard />` — текст усилить
11. `<WebsiteDevelopmentCard />` — позиционировать как доп. услугу (подпись «Дополнительные услуги»)
12. **NEW** `<FinalCta />` — финальный CTA
13. `<Testimonials />` — оставить (между Mobile и Pricing? — оставлю текущее место после FinalCta опционально, см. ниже)
14. `<Footer />`, `<ScrollToTop />`

**Уточнение по `Testimonials`:** оставляю перед `Footer` (после `FinalCta`) — отзывы как социальное доказательство в конце, не перегружая середину.

## Изменения по файлам

### 1. `src/components/landing/Hero.tsx` — переписать
- Бейдж: «Для учебных центров, ДПО и профобучения»
- H1 без typewriter: «Запустим дистанционное обучение в вашем учебном центре **за 7 дней**» (акцент на «за 7 дней» — `text-primary`)
- Статический подзаголовок: «СИНТАГМА помогает быстро подключить учеников, выдать им курсы, контролировать обучение и вести документы в одной системе. Готовые курсы, ИИ-генерация и магазин курсов уже внутри.»
- 3 преимущества с галочкой (`Check` primary): «Настроим систему под ваш учебный центр» / «Подключите учеников и отправляйте курсы в пару кликов» / «Готовые курсы, ИИ и документы в одном месте»
- Две CTA-кнопки: primary `«Оставить заявку на запуск»` → `/register-organization`, outline `«Посмотреть демо»` → `/demo-join`
- Подпись: «Старт за 7 дней · Помощь на каждом шаге · Без отдельной платы за внедрение»
- Удалить: массив `subtitles`, ротацию, dot-indicator, `AnimatePresence`. Декор/glows/StarfieldCanvas — оставить как есть.

### 2. **NEW** `src/components/landing/HowItWorks.tsx`
Секция `py-16` светлый фон, в одном `container max-w-6xl`:
- Заголовок: «Как это работает» + подзаголовок «От нажатия кнопки до выданных документов — за 4 шага».
- Карточка `rounded-2xl border bg-card/60 p-8` с цепочкой из 4 шагов (на desktop в строку, на mobile стек): иконки `Wand2`, `UserPlus`, `GraduationCap`, `FileCheck2`, подписи: «Создали курс» / «Добавили ученика» / «Ученик прошёл обучение» / «Документы в порядке». Между шагами `ArrowRight` в `text-muted-foreground`.
- Сетка 4 мини-карточек `md:grid-cols-4 gap-4` под карточкой-цепочкой:
  - «ИИ-генерация курсов» (`Sparkles`)
  - «Магазин готовых курсов» (`Store`)
  - «Документы для учебного центра» (`FileText`)
  - «Контроль прохождения» (`BarChart3`)
  Стиль: `rounded-xl border bg-card/60 p-5`, иконка в `rounded-full bg-primary/10 text-primary` 40×40.

### 3. `src/components/landing/EditorAndFrdoSections.tsx` — разделить
Разбить на два самостоятельных компонента, чтобы вставлять их в нужные позиции:
- **NEW** `src/components/landing/EditorDemoSection.tsx` — секция «Создавайте курсы за минуты»; подзаголовок заменить на: «Блочный редактор с ИИ-генерацией и профессиональной озвучкой помогает быстро подготовить учебные материалы под вашу программу.» Использует `<EditorDemoCard />` без изменений.
- **NEW** `src/components/landing/FrdoSection.tsx` — секция с `<FrdoPainSlide />`; добавить новый заголовок и подзаголовок над слайдом:
  - H2: «ФИС ФРДО без ручной чистки таблиц и скрытых ошибок»
  - Sub: «СИНТАГМА помогает подготовить данные к выгрузке: форматирует СНИЛС, даты и ФИО, очищает скрытые пробелы и проверяет заполненность перед отправкой.»
  Пункты/кнопки внутри `FrdoPainSlide` (Авто-форматирование СНИЛС/дат/ФИО, очистка пробелов, проверка полноты, шаблоны 35/41, «Проверить файл», «Подробнее про ФРДО») — оставить как есть (если их там сейчас нет — НЕ трогаю содержимое слайда, текст уже отражён).
- Старый файл `EditorAndFrdoSections.tsx` удалить (через `mv`/`rm` в build mode), импорт в `Index.tsx` заменить.

### 4. `src/components/landing/Features.tsx` — усилить
- H2: «Все инструменты учебного центра в одной платформе»
- Sub: «Дистанционное обучение, документооборот, видеоидентификация, ФИС ФРДО и контроль прохождения — без разрозненных сервисов.»
- Карточки оставить как есть. Добавить раскрытие краткого описания по клику: локальный `useState<string|null>(expandedId)`, по клику на карточку — toggle, под заголовком плавно (`framer-motion` height/opacity) появляется уже существующее описание. Иконки/иерархия не меняются.

### 5. `src/components/landing/RostechnadzorCourses.tsx` — усилить тексты
- H2: «300+ готовых курсов для быстрого запуска обучения»
- Sub: «Подключите готовые программы к своей организации и запускайте обучение без разработки контента с нуля.»
- Статистику (300+ курсов / 14 направлений / 24/7) — оставить.

### 6. `src/components/landing/PricingPlans.tsx` — мелкая правка
- Добавить подпись над сеткой тарифов: «Начните бесплатно и масштабируйтесь по мере роста учебного центра.»
- Цены, состав тарифов, выделение «Стандарт» — не трогать.

### 7. `src/components/landing/BoxedVersionCard.tsx` — усилить тексты
- H: «Коробочная версия для организаций с собственной инфраструктурой»
- Sub: «Установим СИНТАГМУ на ваш сервер, адаптируем под требования организации и поможем с интеграцией ваших документов.»
- Пункты: оставить/выровнять под список из ТЗ. Цена 540 000 ₽ и кнопка «Связаться с нами» — без изменений.

### 8. `src/components/landing/WebsiteDevelopmentCard.tsx` — позиционирование
- Над блоком небольшая подпись-eyebrow: «Дополнительная услуга».
- H: «Сайт для образовательной организации под ключ»
- Sub: «Разработаем сайт учебного центра с каталогом курсов, формами заявок и структурой, соответствующей требованиям к образовательным организациям.»
- Цена 55 000 ₽, кнопки «Заказать» / «Подробнее об услуге» — без изменений.

### 9. **NEW** `src/components/landing/FinalCta.tsx`
Спокойная секция `py-20` с центрированным контентом в `max-w-3xl`:
- H2: «Хотите запустить дистанционное обучение без хаоса?»
- Sub: «Оставьте заявку — покажем, как СИНТАГМА может работать именно в вашем учебном центре.»
- Кнопки: primary `«Оставить заявку на запуск»` → `/register-organization`, outline `«Посмотреть демо»` → `/demo-join`.
- Лёгкий циановый gradient-border/glow, без агрессии.

### 10. `src/pages/Index.tsx` — пересборка
Поменять импорты и порядок секций согласно структуре выше. SEO (`<Helmet>`) и сам `<Hero showStars={false} />` оставляю.

## Что НЕ трогаю
- Дизайн-токены, цвета, шрифты, `tailwind.config.ts`, `index.css`.
- `LandingHeader`, `Footer`, `ScrollToTop`, `Testimonials`, `MobileApp` (только если потребуется поменять место — оставляю на текущей позиции после `FinalCta` → перед `Footer`).
- Содержимое `FrdoPainSlide`, `EditorDemoCard`, `PricingPlans` (только обёртка/подпись), `BoxedVersionCard`/`WebsiteDevelopmentCard` (только тексты), `RostechnadzorCourses` (только тексты).

## Открытые вопросы
1. **«Посмотреть демо»** — ставлю на `/demo-join` (есть в роутах). Если есть отдельный лендинг демо/якорь видео — скажите, заменю.
2. **`Testimonials`** — оставить перед `Footer` (после `FinalCta`)? Или убрать совсем, чтобы финальный CTA был последним перед футером?
3. **Раскрытие карточек в `Features`** — делать клик-toggle, как описано, или ограничиться обновлением только заголовка/подзаголовка без интерактива?
