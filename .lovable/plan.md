

## Что осталось из плана аудита конструктора

Реализовано:
- 🔥 Все критичные фиксы (циркулярные зависимости, sync редактора, draft autosave, Ctrl+Z, toast при >10МБ)
- 🎯 Все «быстрые победы» (превью урока, импорт .docx, drag&drop, дублирование блоков, заголовки callout, счётчик слов)

Осталось из категории **⏳ Тех. долг и полировка**:

### 🟢 Мелкие улучшения UX (быстро)

1. **Валидация ссылки на видео в LessonEditor** (#9) — показывать «не распознано» если URL не YouTube/VK/mp4
2. **Confirm при удалении непустой колонки/строки таблицы** (#10)
3. **AI alt-текст для картинок** (#17) — кнопка «Сгенерировать описание» через `generate-image-alt`
4. **Именованные пресеты стилей** (#20) — prompt при сохранении пресета
5. **Поиск Ctrl+F внутри урока** (#13) — панель поиска с подсветкой
6. **Toast «Выделите текст»** при попытке применить стиль без selection (#7 краткосрочно)

### 🟣 Рефакторинг (тех. долг, не виден заказчику)

7. **`RichTextEditor.tsx` 786 → ~200 строк** — разнести на toolbar/palette/link/sanitize
8. **`MediaBlocks.tsx` 595 → 5 файлов** — Image/Video/Audio/Document/DirectVideo
9. **`BlockRenderer.tsx` switch → registry** — `Record<BlockType, FC>`
10. **Хук `useBlockAIGenerate`** — убрать дубли AI-логики из 6 блоков
11. **Убрать дублирование parseContent в useLessonEditor** (#24)

### 🔴 Большой проект (отдельное согласование)

12. **Миграция RichTextEditor с execCommand на TipTap** — 2-3 дня работы, но навсегда решает проблемы Safari/iPad/Firefox

---

### Что предлагаю сделать сейчас

**Вариант А — «Полировка перед сдачей» (1 итерация)**
Пункты 1, 2, 3, 6 — заметные мелочи, заказчик оценит. ~30 мин работы.

**Вариант Б — «Тех. долг» (1-2 итерации)**
Пункты 7, 8, 9, 10 — рефакторинг. Заказчик не увидит, но дальнейшая поддержка будет в 2 раза быстрее.

**Вариант В — «Поиск + расширения» (1 итерация)**
Пункты 4, 5 — добавить именованные пресеты и поиск Ctrl+F. Полезно для длинных уроков.

**Вариант Г — TipTap миграция**
Большой отдельный проект. Делать только если есть запас времени до сдачи.

Какой вариант запускаем? Можно несколько подряд.

