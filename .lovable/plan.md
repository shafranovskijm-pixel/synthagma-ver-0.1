

## План: Добавить второй счётчик Яндекс.Метрики

Добавить счётчик `107590605` рядом с существующим `105216554` в `index.html`.

### Изменения

**`index.html`**:
- В `<head>`: добавить второй `<script>` блок с `ym(107590605, 'init', ...)` после существующего счётчика (с той же проверкой `Capacitor`)
- В `<body>`: добавить второй `<noscript><img>` для счётчика `107590605` рядом с существующим

