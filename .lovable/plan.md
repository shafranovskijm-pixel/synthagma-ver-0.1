

# Исправление экспорта ФИС ФРДО: 100% совпадение с шаблоном

## Проблемы для исправления

### 1. Опечатка в заголовке колонки
Колонка 12 (DPO): `"Наименование квалификации, разреза, класса, категории (при наличии)"` -- опечатка "разреза", правильно **"разряда"**.

### 2. Формат даты
Функция `formatDateForFRDO` возвращает формат `M/d/yy` (американский), нужен формат `dd.MM.yyyy` (российский стандарт для ФРДО).

### 3. Три новые настройки курса
Настройки уже добавлены в БД и UI (предыдущий шаг). Нужно убедиться, что они корректно подставляются при экспорте.

---

## Технические детали

### Файл: `src/utils/frdoExcelExport.ts`

1. **Строка 17** -- исправить "разреза" на "разряда":
   ```
   "Наименование квалификации, разряда, класса, категории (при наличии)"
   ```

2. **Функция `formatDateForFRDO`** (строка 292-298) -- изменить формат:
   ```typescript
   export function formatDateForFRDO(dateStr: string): string {
     if (!dateStr) return "";
     try {
       return format(new Date(dateStr), "dd.MM.yyyy");
     } catch {
       return dateStr;
     }
   }
   ```

3. **Проверка индексов валидации DPO_VALIDATIONS** -- убедиться что все индексы колонок (1-based) соответствуют порядку заголовков:
   - Col 1: Вид документа -- list
   - Col 2: Статус документа -- list
   - Col 3: Подтверждение утраты -- list
   - Col 8: Дополнительная профессиональная программа -- list
   - Col 13: Уровень образования -- list
   - Col 24: Пол получателя -- list (пересчитать! header index 28 = col 29 "Пол получателя")

**Важно:** Нужно пересчитать индексы валидации. Текущие DPO заголовки:
- Col 29 = "Пол получателя" (НЕ 24)
- Col 31 = "Форма обучения" (НЕ 26)
- Col 32 = "Источник финансирования обучения" (НЕ 27)
- Col 33 = "Форма получения образования..." (НЕ 28)

Текущие индексы валидации **НЕПРАВИЛЬНЫЕ** -- нужно пересчитать по 39 колонкам:

| Колонка | Номер (1-based) | Валидация |
|---------|-----------------|-----------|
| Вид документа | 1 | OK |
| Статус документа | 2 | OK |
| Подтверждение утраты | 3 | OK |
| Дополнительная проф. программа | 8 | OK |
| Уровень образования | 13 | OK |
| Пол получателя | 29 | Исправить (было 24) |
| Форма обучения | 31 | Исправить (было 26) |
| Источник финансирования | 32 | Исправить (было 27) |
| Форма получения образования | 33 | Исправить (было 28) |

Нет -- подождите. DPO_HEADERS имеет 39 элементов (строки 6-44). Посчитаю внимательно:

1: Вид документа
2: Статус документа
3: Подтверждение утраты
4: Серия документа
5: Номер документа
6: Дата выдачи документа
7: Регистрационный номер документа
8: Дополнительная профессиональная программа
9: Наименование дополнительной проф. программы
10: Наименование области проф. деятельности
11: Укрупненные группы специальностей
12: Наименование квалификации...
13: Уровень имеющегося образования (ВО/СПО)
14: Фамилия в документе о ВО или СПО
15: Серия документа о ВО/СПО
16: Номер документа о ВО/СПО
17: Год начала обучения
18: Год окончания обучения
19: Срок обучения, часов
20: Фамилия получателя
21: Имя получателя
22: Отчество получателя
23: Дата рождения получателя
24: Пол получателя
25: СНИЛС
26: Форма обучения
27: Источник финансирования обучения
28: Форма получения образования...
29: Гражданство получателя
30-38: Поля оригинала
39: Номер документа для изменения

Итого -- индексы DPO_VALIDATIONS **ПРАВИЛЬНЫЕ**: col 24 = Пол, col 26 = Форма обучения, col 27 = Источник финансирования, col 28 = Форма получения.

### Итоговые изменения

Только 2 исправления в файле `src/utils/frdoExcelExport.ts`:
1. Строка 17: "разреза" -> "разряда"
2. Строки 295: формат даты `"M/d/yy"` -> `"dd.MM.yyyy"`

