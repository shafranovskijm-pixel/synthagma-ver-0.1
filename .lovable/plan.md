
# Обновление профиля ученика: баннер, тема, партнёрская программа

## Задачи

### 1. Баннер (header) в профиле ученика
Сейчас в профиле вверху — карточка с аватаром и небольшим градиентом. Нужно заменить на полноценный приветственный баннер с визуальной темой (как `OrgBanner`, но для ученика). Баннер показывает фото из выбранной темы (по умолчанию — «Офис с зеленью»), приветствие «Добро пожаловать, {имя}!» и название организации.

**Файл**: `src/pages/StudentProfile.tsx` — заменить блок `h-24 bg-gradient-to-r` на компонент-баннер с поддержкой тем.

### 2. Тема оформления — перенести в «Настройки» внутри профиля
Вкладка «Тема» остаётся в сайдбаре как есть, но `ThemeSelector` также встроить прямо в раздел «Профиль» ниже (в настройках). Фактически тема уже в профиле во вкладке — оставить как есть, это уже реализовано.

### 3. Партнёрская программа — визуальное улучшение + оферта внизу
Переработать `StudentPartnerTab.tsx`:
- Добавить привлекательный hero-блок сверху с градиентом и иконками (комиссия, срок, условия)
- 3 карточки преимуществ: «10-25% комиссии», «2 года выплат», «Простое подключение»
- Форма заявки ниже (как сейчас, но с улучшенным оформлением)
- Под формой — блок «Оферта партнёрской программы» с текстом условий (раскрывающийся аккордеон или просто текст)

**Файл**: `src/components/student/StudentPartnerTab.tsx`

## Файлы

| Файл | Действие |
|---|---|
| `src/pages/StudentProfile.tsx` | Заменить простой градиент на тематический баннер с приветствием |
| `src/components/student/StudentPartnerTab.tsx` | Визуальная переработка + оферта внизу |
