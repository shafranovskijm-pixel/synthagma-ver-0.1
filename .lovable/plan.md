План настройки Novofon до рабочего прозвона:

1. Безопасно сохранить данные Novofon в backend-секреты:
   - `NOVOFON_ACCESS_TOKEN` и `NOVOFON_DATA_ACCESS_TOKEN` = Secret из вкладки API.
   - `NOVOFON_VIRTUAL_PHONE_NUMBER` / `NOVOFON_CALLER_ID` = `79675551383`.
   - `NOVOFON_OPERATOR_NUMBER` = `79147213424` для тестового менеджерского звонка.
   - SIP-данные (`NOVOFON_SIP_LOGIN`, `NOVOFON_SIP_PASSWORD`) сохранить для резервных сценариев, не выводя их в интерфейс/логи.

2. Исправить backend-интеграцию под официальный ответ Novofon:
   - использовать именно `Secret` как `access_token` для Data API и Call API;
   - убрать зависимость от отдельного API_SECRET для JSON-RPC;
   - добавить диагностический вызов `get.employees`, чтобы получить/проверить `employee_id` сотрудника;
   - если Novofon требует `employee_id`, добавить fallback на `start.employee_call` после `start.simple_call`.

3. Обновить диагностику в интерфейсе:
   - показывать проверку Data API, список сотрудников/employee_id, купленный номер, Call API и тестовый звонок;
   - оставить номер ввода с `+7` по умолчанию;
   - сообщения об ошибках сделать конкретными: неправильный Secret, нет доступа Call API/Data API, неверный номер оператора, собственный виртуальный номер.

4. Задеплоить Edge-функции Novofon.

5. Выполнить end-to-end проверку:
   - вызвать диагностику Novofon;
   - сделать реальный прозвон на `+79147213424`;
   - проверить, что Novofon вернул успешный `call_session_id`/статус и запись появилась в журнале звонков.

Технически я не буду публиковать в чат ваш Secret/SIP-пароль; они будут использоваться только как защищённые backend-секреты.