
Юзер показал: floating toolbar форматирования сейчас всё ещё появляется по центру выделения / по центру блока, а должен — слева, на той же вертикали, что и круглая «+» кнопка слева от блока (см. скрин). То есть toolbar должен «жить» в левой gutter-зоне рядом с блоком, а не над/под текстом по центру.

Смотрю текущую реализацию `RichTextEditor.tsx`, чтобы понять, где задаётся позиция и почему `left: rect.left` всё равно даёт визуально центр.
